7) Escreva um programa em linguagem C que receba dois números inteiros diferentes digitados pelo usuário e imprima o maior deles.
#include <stdio.h>

int main() {
    int num1, num2;

    printf("Digite o primeiro número inteiro: ");
    scanf("%d", &num1);

    printf("Digite o segundo número inteiro (diferente do primeiro): ");
    scanf("%d", &num2);

    if (num1 == num2) {
        printf("Os números devem ser diferentes!\n");
    } else {
        
        if (num1 > num2) {
            printf("O maior número é: %d\n", num1);
        } else {
            printf("O maior número é: %d\n", num2);
        }
    }

    return 0;
}
